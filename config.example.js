window.PESQUISA_CONFIG = {
  /** Título no cabeçalho */
  surveyTitle: 'Antecipação de Recebíveis',

  /** URL do Apps Script implantado (termina em /exec) — veja SETUP.md */
  appsScriptUrl: '',

  /**
   * Token compartilhado com o Apps Script (reduz spam se a URL vazar).
   * Deixe vazio só em ambiente de teste local.
   */
  submitToken: '',

  /** URL opcional de logo (https). Se vazio, mostra só o título. */
  logoUrl: ''
};
